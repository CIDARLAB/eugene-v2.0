/*** WORKS!
// primitives
num[] na = [1, 2, 3];
println(na);

na[0] = 3;
na[1] = 2;
na[2] = 1;

println(na);
 ***/
 

// parts
Property pna(num[]);
PartType P(pna);

P p1([1, 2, 3]);
P p2([3, 2, 1]);

p1 = p2;
println(p1);
println(p2);

/**
println(p1.pna);
p1.pna[0] = 10;
println(p1.pna);
 **/